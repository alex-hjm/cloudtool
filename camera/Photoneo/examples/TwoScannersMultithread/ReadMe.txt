========================================================================
    CONSOLE APPLICATION : TwoScannersMultithreadExample Project Overview
========================================================================

Console application which shows how to use API when handling multiple scanners in multi-thread application. There is one thread which triggers scans, one which acquires frames and one control thread which notifies other threads what to do.

/////////////////////////////////////////////////////////////////////////////
